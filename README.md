# Проект E-commerce Core

## Описание

Этот проект представляет собой ядро для интернет-магазина, реализованное с применением объектно-ориентированного программирования (ООП). Основные сущности проекта — классы **Product** (товары) и **Category** (категории товаров), а также наследники товаров: **Smartphone** (смартфон) и **LawnGrass** (трава газонная).

В проекте реализованы базовые функции работы с товарами и категориями: хранение атрибутов, подсчёт количества продуктов, строковое представление, обработка сложения товаров с ограничением на комбинации классов и защита добавления объектов в категории.

Проект подготовлен к расширению и интеграции с внешними интерфейсами (веб-приложения, телеграм-боты и др.).

***

## Функциональность, реализованная в проекте

- Класс `Product` с приватным атрибутом цены, геттерами/сеттерами, методами для отображения и сложения товаров.
- Классы-наследники:
    - `Smartphone` — с расширенными атрибутами: производительность, модель, память, цвет.
    - `LawnGrass` — с расширенными атрибутами: страна-производитель, срок прорастания, цвет.
- Метод сложения `__add__` реализован с проверкой, что складывать можно только одинаковые классы товаров (выбрасывается `TypeError` при нарушении).
- Класс `Category` с хранением списка товаров, защитой добавления только объектов типа `Product` или его наследников.
- Итератор `CategoryIterator` для перебора товаров внутри категории.
- Возможность загрузки товаров из JSON (реализация в проекте).
- Полное покрытие функционала тестами (более 75%) с использованием pytest.

***

## Технологии и инструменты

- Python 3.13+
- Работа с виртуальными окружениями (Poetry)
- Чистый код по стандарту PEP 8 (flake8)
- Форматирование кода (black, isort)
- Статический анализ и проверка типов (mypy)
- Модульное тестирование (pytest, pytest-cov)

***

## Структура проекта

```
OOP_HW1/
│
├── main.py                   # Точка входа в приложение, демонстрация работы классов
├── src/
│   └── product.py            # Классы продукта, наследники и категория
├── tests/
│   └── test_product.py       # Модуль тестов, проверяющих функциональность
├── README.md                 # Документация проекта
├── pyproject.toml            # Конфигурация Poetry и зависимостей
└── ...
```


***

## Запуск и тестирование

- Установка зависимостей и создание виртуального окружения:

```bash
poetry install
poetry shell
```

- Запуск тестов с отчётом покрытия:

```bash
pytest --cov=src --cov-report=term --cov-report=html
```

- Открыть `htmlcov/index.html` для просмотра отчёта по покрытию.

***